{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "R -e \"devtools::document(roclets = c('rd', 'collate', 'namespace', 'vignette'))\"; R CMD INSTALL --no-multiarch --with-keep.source .",

            "presentation": {
                "reveal": "never"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
        
    ]
}